# 核心依赖 (所有平台通用)
# ============================

# --- API 服务 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- HTTP 客户端 ---
httpx>=0.27.0
openai>=1.30.0

# --- 数据处理 ---
pandas>=2.2.0
numpy>=1.26.0

# --- 工具 ---
python-dotenv>=1.0.0
rich>=13.0.0
typer>=0.12.0
tiktoken>=0.7.0

# --- 测试 ---
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ============================
# RAG 与检索 (Phase 3 需要)
# ============================
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0      # CPU 版，所有平台可用
chromadb>=0.5.0
pymupdf>=1.24.0       # PDF 解析
python-docx>=1.1.0    # Word 解析
langchain>=0.2.0
langgraph>=0.2.0
llama-index>=0.10.0
ragas>=0.1.0          # RAG 评测

# ============================
# 深度学习 (Phase 2 需要)
# ============================
# CPU/MPS(Apple Silicon)/CUDA 通用
torch>=2.1.0
torchvision>=0.16.0

# ============================
# 本地 LLM (Phase 2/4 需要，按需安装)
# ============================
# mlx>=0.16.0          # 🍎 仅 Apple Silicon，Windows/Linux 跳过
# mlx-lm>=0.18.0       # 🍎 仅 Apple Silicon
# vllm>=0.6.0          # 🖥️ 仅 Linux+Nvidia GPU+CUDA 12.1+

# ============================
# 数据库 (Phase 3/6 需要)
# ============================
redis>=5.0.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# ============================
# Transformers (Phase 2/4 需要)
# ============================
transformers>=4.40.0
# peft>=0.12.0          # LoRA/QLoRA 微调 (Phase 2)
# bitsandbytes>=0.43.0  # QLoRA 量化训练 (仅 Linux/Windows+Nvidia GPU)
